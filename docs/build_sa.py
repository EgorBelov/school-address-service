"""
Скрипт генерации docs/индивидуальная_системный_аналитик.docx —
индивидуальная часть отчёта для роли «Системный аналитик».
"""
from pathlib import Path

from report_lib import (
    add_bullet,
    add_heading,
    add_note,
    add_para,
    add_table_simple,
    add_title_page,
    new_document,
)


REPORT_PATH = Path(__file__).parent / "индивидуальная_системный_аналитик.docx"


doc = new_document()
add_title_page(doc, "Индивидуальная часть.\nСистемный аналитик")


# ─────────────────────── Введение ────────────────────────────

add_heading(doc, "Введение", level=1)

add_note(
    doc,
    "Кратко перечислить, какие задачи решал системный аналитик и какие "
    "артефакты будут описаны в индивидуальной части. Объём 0,5–1 страница."
)

add_para(
    doc,
    "Настоящая индивидуальная часть отчёта посвящена работе, выполненной "
    "в роли системного аналитика. В обязанности системного аналитика "
    "входили: формализация функциональных и нефункциональных требований "
    "к программному обеспечению на основе бизнес-требований, "
    "построение каталога прецедентов и UML-диаграмм (использования, "
    "компонентов, последовательностей, развёртывания), проектирование "
    "модели данных (ER-диаграмма), разработка прототипов "
    "пользовательского интерфейса, сопровождение глоссария проекта. "
    "Все перечисленные артефакты последовательно представлены ниже."
)


# ─────────────────────── 1. Функциональные требования ────────────────────────────

add_heading(doc, "1. Каталог функциональных требований (FR)", level=1)

add_note(
    doc,
    "Каталог FR с приоритетом по MoSCoW (Must / Should / Could / Won't) и "
    "трассировкой к бизнес-требованиям из части бизнес-аналитика. "
    "Объём 1–1,5 страницы."
)

add_para(
    doc,
    "На основе бизнес-требований, сформулированных бизнес-аналитиком, "
    "разработан каталог из 11 функциональных требований с указанием "
    "приоритета по методологии MoSCoW и трассировкой к "
    "соответствующему бизнес-требованию. Каталог приведён в "
    "таблице 1.1."
)
add_para(doc, "Таблица 1.1 — Каталог функциональных требований",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["ID", "Требование", "Приоритет", "Связан с"],
    ["F-01", "Принимать постановления в форматах .doc, .docx, .pdf",
     "Must",   "BR-04, BR-07"],
    ["F-02", "Извлекать текстовое содержимое документа, при необходимости — через OCR",
     "Must",   "BR-04, BR-07"],
    ["F-03", "Автоматически определять структуру табличной части документа",
     "Must",   "BR-04"],
    ["F-04", "Извлекать метаданные постановления: номер, дату, муниципалитет",
     "Should", "BR-04"],
    ["F-05", "Извлекать перечень школ и для каждой — список правил «улица — дома»",
     "Must",   "BR-03, BR-04"],
    ["F-06", "Нормализовать правила: тип, чётность, диапазоны, исключения, точные списки",
     "Must",   "BR-03"],
    ["F-07", "Сохранять извлечённые данные в реляционной базе данных",
     "Must",   "BR-04, BR-05"],
    ["F-08", "Предоставлять веб-форму поиска школы по адресу с автокомплитом",
     "Must",   "BR-01, BR-02"],
    ["F-09", "Возвращать пользователю название школы, её адрес и текст правила-основания",
     "Must",   "BR-01, BR-03"],
    ["F-10", "Предоставлять административную панель: загрузка, парсинг, сохранение, "
             "CRUD правил, отчёт валидатора",
     "Should","BR-05"],
    ["F-11", "Автоматически выявлять пересечения правил между разными школами "
             "на одной улице",
     "Should","BR-06"],
])


# ─────────────────────── 2. Нефункциональные требования ────────────────────────────

add_heading(doc, "2. Каталог нефункциональных требований (NFR)", level=1)

add_note(
    doc,
    "NFR с критериями приёмки: как именно будет проверяться выполнение. "
    "Объём 0,5–1 страница."
)

add_para(
    doc,
    "К программному обеспечению предъявлены следующие нефункциональные "
    "требования с измеримыми критериями приёмки."
)
add_para(doc, "Таблица 2.1 — Каталог нефункциональных требований",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["ID", "Требование", "Критерий приёмки"],
    ["NF-01", "Время отклика на поиск школы — не более 1 с при ≤ 1000 правил",
     "Замер на тестовой БД с 700 правилами: P95 ≤ 1 с"],
    ["NF-02", "Устойчивость к недоступности DaData",
     "При отключённом DaData поиск возвращает корректный ответ через "
     "локальный regex-парсер"],
    ["NF-03", "Устойчивость к недоступности GigaChat",
     "При transient-ошибках выполняется ретрай с экспоненциальным "
     "backoff; всего до 4 попыток"],
    ["NF-04", "Открытый стек технологий",
     "Только Python, FastAPI, SQLAlchemy, SQLite и Open Source-библиотеки; "
     "без проприетарных зависимостей"],
    ["NF-05", "Модульность", "Каждый сервис изолирован в отдельный пакет "
     "app/services/<domain>/"],
    ["NF-06", "Покрытие тестами",
     "Ключевая «чистая» логика покрыта модульными тестами; "
     "основные сценарии — интеграционными"],
])


# ─────────────────────── 3. Use Case-диаграмма ────────────────────────────

add_heading(doc, "3. Диаграмма прецедентов (Use Case Diagram)", level=1)

add_note(
    doc,
    "В этом разделе ожидается UML Use Case Diagram. Лучше нарисовать в "
    "PlantUML / draw.io и вставить картинкой. Здесь приведено "
    "текстовое описание актёров и связей. Объём 0,5 страницы."
)

add_para(doc, "Идентифицированы 2 актёра системы:", first_line_indent=0)
for item in [
    "«Родитель» — анонимный пользователь, обращающийся к публичной "
    "части сервиса для определения «своей» школы;",
    "«Администратор УО» — авторизованный пользователь (в будущем; в "
    "прототипе авторизация отсутствует), управляющий загрузкой "
    "постановлений и редактированием правил.",
]:
    add_bullet(doc, item)

add_para(
    doc,
    "Прецеденты «Поиск школы по адресу» и «Автодополнение адреса» "
    "доступны актёру «Родитель»; прецеденты «Загрузка постановления», "
    "«Запуск парсинга», «Сохранение в БД», «Просмотр и редактирование "
    "правил», «Просмотр отчёта валидатора» — актёру «Администратор УО». "
    "Прецедент «Автодополнение адреса» расширяет (extend) прецедент "
    "«Поиск школы по адресу»; прецедент «Запуск парсинга» включает "
    "(include) обращение к внешним системам DaData (для нормализации) и "
    "GigaChat (для разбора нестандартных документов)."
)


# ─────────────────────── 4. Развёрнутые прецеденты ────────────────────────────

add_heading(doc, "4. Развёрнутые описания прецедентов", level=1)

add_note(
    doc,
    "Привести подробные описания 7 прецедентов по стандартному шаблону: "
    "название, актёр, предусловия, основной сценарий, альтернативные "
    "сценарии, постусловия. Объём 2–2,5 страницы. Ниже — два полностью "
    "проработанных прецедента (UC-1, UC-3) как образец; остальные "
    "оформить по той же структуре."
)

add_para(doc, "UC-1. Поиск школы по адресу", bold=True, first_line_indent=0)
add_table_simple(doc, [
    ["Атрибут", "Значение"],
    ["Актёр",          "Родитель"],
    ["Предусловия",    "Сервис запущен; в БД сохранён хотя бы один decree с правилами"],
    ["Триггер",        "Пользователь открыл главную страницу и нажал «Найти школу»"],
    ["Основной сценарий",
     "1) Пользователь вводит адрес в текстовое поле формы.\n"
     "2) Система отправляет адрес на эндпоинт POST /search.\n"
     "3) Сервис нормализует адрес через DaData (clean).\n"
     "4) Сервис извлекает населённый пункт, улицу и номер дома.\n"
     "5) Сервис обходит правила и применяет три проверки (улица, локалити, дом).\n"
     "6) Сервис возвращает первое совпавшее правило и связанную школу.\n"
     "7) Шаблон выводит пользователю название школы, её адрес и текст правила."],
    ["Альтернативные сценарии",
     "3а) DaData недоступна — выполняется парсинг локальным regex-парсером.\n"
     "5а) Совпадений не найдено — пользователю показывается сообщение «Школа не найдена»."],
    ["Постусловия",    "Состояние БД не изменяется"],
])

add_para(doc, "UC-3. Загрузка постановления", bold=True, first_line_indent=0)
add_table_simple(doc, [
    ["Атрибут", "Значение"],
    ["Актёр",          "Администратор УО"],
    ["Предусловия",    "Доступна административная панель"],
    ["Триггер",        "Администратор открыл /admin/upload и выбрал файл"],
    ["Основной сценарий",
     "1) Администратор выбирает файл (.doc/.docx/.pdf) и отправляет форму.\n"
     "2) Сервис сохраняет файл в storage/decrees/.\n"
     "3) Модуль text_extractor извлекает текст с диспетчеризацией по расширению.\n"
     "4) Для .pdf с малым объёмом текстового слоя (< 500 симв.) применяется OCR.\n"
     "5) Извлечённый текст возвращается администратору вместе с путём к файлу."],
    ["Альтернативные сценарии",
     "3а) Файл .doc — конвертация через LibreOffice (на macOS — fallback на textutil).\n"
     "4а) Ошибка OCR — пользователю выводится сообщение, файл сохранён без текста."],
    ["Постусловия",    "Файл сохранён в storage/decrees/; текст в storage/extracted/"],
])

add_note(
    doc,
    "По аналогии оформить UC-2 (Автодополнение адреса), UC-4 (Запуск "
    "парсинга), UC-5 (Сохранение в БД), UC-6 (Просмотр и редактирование "
    "правил), UC-7 (Просмотр отчёта валидатора)."
)


# ─────────────────────── 5. ER-модель ────────────────────────────

add_heading(doc, "5. Модель данных (ER-диаграмма)", level=1)

add_note(
    doc,
    "ER-диаграмму желательно нарисовать (draw.io / dbdiagram.io) и "
    "вставить картинкой. Здесь приведено текстовое описание сущностей и "
    "связей. Объём 1–1,5 страницы."
)

add_para(
    doc,
    "Модель данных проектировалась с учётом необходимости поддержки "
    "трёх различных форматов постановлений и сохранения исходного "
    "текста правила как страховки на случай ошибок парсинга. "
    "Состав сущностей приведён в таблице 5.1."
)
add_para(doc, "Таблица 5.1 — Сущности модели данных",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["Сущность", "Поле", "Тип", "Назначение"],
    ["Municipality", "id",          "Integer (PK)", "Уникальный идентификатор"],
    ["",             "name",        "String",       "Название муниципалитета"],
    ["",             "region",      "String",       "Регион (опционально)"],
    ["School",       "id",          "Integer (PK)", "Уникальный идентификатор"],
    ["",             "municipality_id", "FK → Municipality", "Связь с муниципалитетом"],
    ["",             "name",        "String",       "Наименование школы"],
    ["",             "address",     "String",       "Юридический адрес школы"],
    ["Decree",       "id",          "Integer (PK)", "Уникальный идентификатор"],
    ["",             "municipality_id", "FK → Municipality", "Связь с муниципалитетом"],
    ["",             "number",      "String",       "Номер постановления"],
    ["",             "date",        "String",       "Дата постановления"],
    ["",             "file_path",   "String",       "Путь к исходному файлу"],
    ["AddressRule",  "id",          "Integer (PK)", "Уникальный идентификатор"],
    ["",             "school_id",   "FK → School",  "Связь со школой"],
    ["",             "decree_id",   "FK → Decree",  "Связь с постановлением"],
    ["",             "locality",    "String",       "Населённый пункт"],
    ["",             "street",      "String",       "Название улицы"],
    ["",             "normalized_street", "String", "Нормализованное название улицы"],
    ["",             "house_rule_raw",    "String", "Исходный текст правила (страховка)"],
    ["",             "rule_type",   "String",       "Тип: range / exact_list / up_to / from_to_end / all"],
    ["",             "parity",      "String",       "Чётность: all / even / odd / mixed / unknown"],
    ["",             "house_from",  "Integer",      "Нижняя граница диапазона"],
    ["",             "house_to",    "Integer",      "Верхняя граница диапазона"],
    ["",             "house_number","String",       "Единственный номер (для одиночных правил)"],
    ["",             "house_numbers","String",      "Точный список номеров через запятую"],
    ["",             "exceptions",  "String",       "Список номеров-исключений"],
    ["",             "comment",     "String",       "Произвольный комментарий"],
])

add_para(doc, "Связи между сущностями:", first_line_indent=0)
for item in [
    "Municipality 1 — N School;",
    "Municipality 1 — N Decree;",
    "School 1 — N AddressRule;",
    "Decree 1 — N AddressRule.",
]:
    add_bullet(doc, item)

add_para(
    doc,
    "Особенностью модели является наличие поля house_rule_raw — это "
    "исходный текст правила из постановления. Даже если структурный "
    "парсер не смог корректно заполнить поля rule_type, parity и "
    "диапазоны, оператор сможет вручную восстановить правило по "
    "сырому тексту через CRUD-таблицу в админ-панели."
)


# ─────────────────────── 6. Диаграмма компонентов ────────────────────────────

add_heading(doc, "6. Диаграмма компонентов", level=1)

add_note(
    doc,
    "UML Component Diagram. Желательно нарисовать в PlantUML. Здесь — "
    "текстовое описание компонентов и связей. Объём 1 страница."
)

add_para(
    doc,
    "Приложение состоит из 8 основных компонентов, изолированных в "
    "отдельные пакеты внутри app/services/. Описание компонентов "
    "приведено в таблице 6.1; в общей части отчёта (раздел 3.2) "
    "представлена верхнеуровневая схема взаимодействия."
)
add_para(doc, "Таблица 6.1 — Компоненты приложения и их связи",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["Компонент", "Назначение", "Зависит от"],
    ["app/main.py",       "Точка входа FastAPI, определение эндпоинтов",
     "Все services/"],
    ["services/parser/",  "Извлечение текста, диспетчер парсинга, стратегии",
     "ai/, dadata/, ocr/"],
    ["services/ai/gigachat/", "Клиент GigaChat, промпты, схемы, retry",
     "—"],
    ["services/dadata/",  "Клиент DaData, сверка улиц",
     "—"],
    ["services/ocr/",     "OCR через ocr.space",
     "—"],
    ["services/address/", "Нормализация адреса, локальный парсер",
     "—"],
    ["services/search/",  "Поиск школы по адресу",
     "address/, dadata/"],
    ["services/validation/", "Валидатор правил",
     "models/"],
    ["db/, models/",      "ORM и доступ к БД",
     "—"],
])


# ─────────────────────── 7. Sequence-диаграммы ────────────────────────────

add_heading(doc, "7. Диаграммы последовательностей", level=1)

add_note(
    doc,
    "UML Sequence Diagram для ключевых сценариев. Желательно нарисовать "
    "3–4 sequence-диаграммы (PlantUML / draw.io) и вставить картинками. "
    "Здесь приведено текстовое описание шагов взаимодействия. "
    "Объём 1,5 страницы."
)

add_para(doc, "Сценарий 7.1. Поиск школы по адресу", bold=True,
         first_line_indent=0)
for item in [
    "1) Пользователь (Browser) → FastAPI: POST /search { address }",
    "2) FastAPI → DaData.clean(address)",
    "3) DaData → FastAPI: нормализованный адрес { city, street, house }",
    "4) FastAPI → AddressRule (БД): SELECT всех правил",
    "5) FastAPI → find_school: применить три фильтра (улица, локалити, дом)",
    "6) find_school → FastAPI: первое совпавшее правило + School",
    "7) FastAPI → Browser: HTML с названием школы и текстом правила",
]:
    add_bullet(doc, item)

add_para(doc, "Сценарий 7.2. Загрузка и парсинг постановления", bold=True,
         first_line_indent=0)
for item in [
    "1) Browser → FastAPI: POST /admin/upload (multipart, file)",
    "2) FastAPI → text_extractor.extract_text(path)",
    "3) text_extractor → pdfplumber/python-docx/LibreOffice: получение текста",
    "4) text_extractor → ocr.space (если |текст| < 500 симв.)",
    "5) FastAPI → Browser: страница с извлечённым текстом",
    "6) Browser → FastAPI: POST /admin/parse-with-ai { text, file_path }",
    "7) FastAPI → universal_parser: структурная детекция формата",
    "8) universal_parser → MultiColumnParser/TwoColumnParser (по результату)",
    "9) Стратегия → GigaChat (только для two-column, для каждой школы)",
    "10) GigaChat → Стратегия: JSON с правилами",
    "11) Стратегия → Pydantic-валидация",
    "12) FastAPI → Browser: JSON распарсенных правил",
    "13) Browser → FastAPI: POST /admin/save-parsed",
    "14) FastAPI → БД: INSERT Municipality/School/Decree/AddressRule",
]:
    add_bullet(doc, item)


# ─────────────────────── 8. Диаграмма развёртывания ────────────────────────────

add_heading(doc, "8. Диаграмма развёртывания", level=1)

add_note(
    doc,
    "UML Deployment Diagram. Здесь — текстовое описание узлов "
    "развёртывания и связей; нарисованный вариант рекомендуется "
    "вставить картинкой. Объём 0,5 страницы."
)

add_para(
    doc,
    "В прототипе развёртывание выполняется на одном узле "
    "(локальная машина разработчика); файловая база SQLite "
    "располагается рядом с приложением. Для производственного "
    "развёртывания узлы разнесены: «Веб-сервер», «БД-сервер», "
    "«Файловое хранилище» (storage/decrees, storage/extracted). "
    "Внешние API (DaData, GigaChat, ocr.space) находятся за пределами "
    "контура развёртывания и доступны через HTTPS."
)


# ─────────────────────── 9. Прототипы интерфейса ────────────────────────────

add_heading(doc, "9. Прототипы пользовательского интерфейса", level=1)

add_note(
    doc,
    "Wireframes 4 экранов: главная (поиск), админ-загрузка, "
    "админ-правила (CRUD), отчёт валидатора. Лучше нарисовать в Figma "
    "/ Balsamiq и вставить картинками. Здесь — описание элементов "
    "интерфейса. Объём 1,5 страницы."
)

add_para(doc, "Экран 9.1. Главная страница (/)", bold=True, first_line_indent=0)
for item in [
    "заголовок «Найти школу по адресу»;",
    "текстовое поле ввода адреса с подсказками DaData (выпадающий список "
    "ниже поля при наборе);",
    "кнопка «Найти»;",
    "блок результата (отображается после поиска): название школы, адрес "
    "школы, текст правила-основания.",
]:
    add_bullet(doc, item)

add_para(doc, "Экран 9.2. Загрузка постановления (/admin/upload)", bold=True,
         first_line_indent=0)
for item in [
    "поле выбора файла (input type=file, accept=.doc,.docx,.pdf);",
    "кнопка «Загрузить и извлечь текст»;",
    "после загрузки — превью извлечённого текста с прокруткой;",
    "кнопка «Распарсить через GigaChat»;",
    "после парсинга — JSON-просмотр распарсенных правил с возможностью "
    "редактировать перед сохранением;",
    "кнопка «Сохранить в БД».",
]:
    add_bullet(doc, item)

add_para(doc, "Экран 9.3. CRUD правил (/admin/rules)", bold=True,
         first_line_indent=0)
for item in [
    "таблица всех правил с колонками: школа, локалити, улица, "
    "house_rule_raw, тип, чётность, диапазон;",
    "форма редактирования каждой строки (inline или модальное окно);",
    "кнопка «Удалить» на строке;",
    "фильтр по школе и по улице в шапке таблицы.",
]:
    add_bullet(doc, item)

add_para(doc, "Экран 9.4. Отчёт валидатора (/admin/validation)", bold=True,
         first_line_indent=0)
for item in [
    "сводка по числу нарушений по типам (5 типов проверок);",
    "детальная таблица нарушений с указанием правила, школы, типа "
    "нарушения и комментария;",
    "цветовая маркировка по уровню (error / warning).",
]:
    add_bullet(doc, item)


# ─────────────────────── 10. Глоссарий ────────────────────────────

add_heading(doc, "10. Глоссарий проекта", level=1)

add_note(
    doc,
    "Глоссарий — словарь предметных и технических терминов. Объём 1 страница."
)

add_para(doc, "Таблица 10.1 — Глоссарий проекта",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["Термин", "Определение"],
    ["Постановление",
     "Муниципальный нормативный правовой акт «О закреплении территорий "
     "за общеобразовательными организациями»"],
    ["Закреплённая территория",
     "Совокупность улиц и номеров домов, обслуживаемых конкретной школой "
     "согласно муниципальному постановлению"],
    ["Правило",
     "Атомарное закрепление вида «улица + описание домов → школа»"],
    ["Чётность (parity)",
     "Признак правила: применяется ко всем домам / только чётным / "
     "только нечётным / смешанный / неизвестно"],
    ["Континуация-строка",
     "Строка таблицы, продолжающая описание правил той же школы, что и "
     "предыдущая (имя школы в ячейке пустое или повторяется)"],
    ["Алиас улицы",
     "Альтернативное (часто историческое) название улицы, указанное в "
     "скобках при основном"],
    ["LLM",
     "Большая языковая модель (large language model); в проекте — "
     "GigaChat от Сбера"],
    ["OCR",
     "Optical Character Recognition; распознавание текста из "
     "изображения. В проекте — облачный сервис ocr.space"],
    ["DaData",
     "Российский сервис нормализации и обогащения адресов"],
    ["Pydantic",
     "Python-библиотека валидации данных на основе типизированных моделей"],
    ["Толерантный матч локалити",
     "Алгоритм сопоставления названий населённых пунктов, "
     "устойчивый к падежным окончаниям и букве «ё»"],
])


# ─────────────────────── 11. Заключение ────────────────────────────

add_heading(doc, "11. Заключение", level=1)

add_para(
    doc,
    "В индивидуальной части системного аналитика разработан полный "
    "комплект проектной документации: каталог 11 функциональных и "
    "6 нефункциональных требований с критериями приёмки, диаграмма "
    "прецедентов и развёрнутые описания 7 use-кейсов, модель данных "
    "из 4 сущностей с детальным описанием полей, диаграммы компонентов, "
    "последовательностей и развёртывания, прототипы 4 экранов "
    "пользовательского интерфейса, глоссарий проекта. Разработанная "
    "документация послужила основой для реализации сервисной и "
    "интеллектуальной частей системы и для проектирования стратегии "
    "тестирования."
)


doc.save(REPORT_PATH)
print(f"Готово: {REPORT_PATH}")
