"""
Скрипт генерации docs/индивидуальная_разработчик_сервиса.docx —
индивидуальная часть отчёта для роли «Разработчик сервиса».

ВАЖНО: этот отчёт НЕ включает разделы про работу с LLM (GigaChat),
промпты, Pydantic-схемы LLM, retry, регэкс-fallback для LLM — всё это
описано в индивидуальной части разработчика ИИ-агентов (build_ai.py).
Также не включает разделы про тестирование — они в build_qa.py.
"""
from pathlib import Path

from report_lib import (
    add_bullet,
    add_heading,
    add_note,
    add_para,
    add_table_simple,
    add_title_page,
    new_document,
)


REPORT_PATH = Path(__file__).parent / "индивидуальная_разработчик_сервиса.docx"


doc = new_document()
add_title_page(doc, "Индивидуальная часть.\nРазработчик сервиса")


# ─────────────────────── Введение ────────────────────────────

add_heading(doc, "Введение", level=1)

add_note(
    doc,
    "Кратко перечислить, какие задачи решал разработчик сервиса. "
    "Важно подчеркнуть, что вопросы интеграции с LLM рассмотрены в "
    "индивидуальной части разработчика ИИ-агентов, а тестирование — "
    "в индивидуальной части тестировщика."
)

add_para(
    doc,
    "Настоящая индивидуальная часть отчёта посвящена работе, выполненной "
    "в роли разработчика сервиса. В обязанности разработчика сервиса "
    "входило проектирование и реализация серверной части веб-приложения "
    "на фреймворке FastAPI: каркас приложения, модель данных и доступ к "
    "базе, извлечение текста из документов разных форматов, реализация "
    "структурных стратегий парсинга таблиц без обращения к большой "
    "языковой модели, нормализация правил, регэксповый fallback, "
    "интеграция с DaData, алгоритм поиска школы по адресу, валидатор "
    "правил и административная панель. Вопросы интеграции с GigaChat, "
    "инжиниринга промптов и работы с ответами LLM рассмотрены в "
    "индивидуальной части разработчика ИИ-агентов; вопросы "
    "тестирования — в индивидуальной части тестировщика."
)


# ─────────────────────── 1. Структура проекта ────────────────────────────

add_heading(doc, "1. Структура проекта", level=1)

add_note(
    doc,
    "Привести дерево директорий с краткими комментариями к каждому "
    "пакету. Объём 1 страница."
)

add_para(
    doc,
    "Реализована послойная структура каталога app/ с изоляцией каждой "
    "доменной подсистемы в отдельный пакет в директории services/<domain>/. "
    "Структура исходного кода приведена в таблице 1.1."
)
add_para(doc, "Таблица 1.1 — Структура исходного кода",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["Путь", "Назначение"],
    ["app/main.py",                          "FastAPI-приложение, все эндпоинты"],
    ["app/core/config.py",                   "Pydantic Settings, чтение .env"],
    ["app/db/session.py",                    "SQLAlchemy engine + SessionLocal"],
    ["app/models/models.py",                 "ORM-модели: Municipality, School, Decree, AddressRule"],
    ["app/services/address/normalize.py",    "Нормализация улицы и локалити"],
    ["app/services/address/local_parser.py", "Regex-парсер адреса (fallback к DaData)"],
    ["app/services/dadata/client.py",        "Клиент DaData (suggest, clean)"],
    ["app/services/dadata/street_validator.py","Сверка улицы с DaData (с lru_cache)"],
    ["app/services/ocr/ocr_space.py",        "OCR через сервис ocr.space"],
    ["app/services/parser/text_extractor.py","Диспетчер извлечения текста"],
    ["app/services/parser/splitter.py",      "Разбиение текста на чанки по школам"],
    ["app/services/parser/metadata_extractor.py","Regex-извлечение номера/даты/муниципалитета"],
    ["app/services/parser/pdf_table_extractor.py","Двухколоночные таблицы PDF"],
    ["app/services/parser/docx_table_extractor.py","Таблицы DOCX"],
    ["app/services/parser/multi_column_extractor.py","4-колоночные таблицы"],
    ["app/services/parser/territory_regex_fallback.py","Regex-добор улиц"],
    ["app/services/parser/rule_normalizer.py","Нормализация правил"],
    ["app/services/parser/save_parsed_decree.py","Сохранение в БД"],
    ["app/services/parser/universal_parser.py","Диспетчер стратегий"],
    ["app/services/parser/strategies/two_column_parser.py","Стратегия двухколоночных таблиц"],
    ["app/services/parser/strategies/multi_column_parser.py","Стратегия многоколоночных таблиц"],
    ["app/services/search/find_school.py",   "Алгоритм поиска школы"],
    ["app/services/validation/rules_validator.py","Валидатор правил"],
    ["app/templates/",                       "HTML-шаблоны Jinja2"],
    ["storage/decrees/",                     "Хранилище исходных файлов постановлений"],
    ["storage/extracted/",                   "Извлечённый текст (.txt)"],
    ["seed.py / reset_db.py / inspect_db.py","CLI-утилиты"],
])


# ─────────────────────── 2. Модель данных и доступ к БД ────────────────────────────

add_heading(doc, "2. Реализация модели данных и доступа к БД", level=1)

add_note(
    doc,
    "Описание ORM-моделей и подхода к доступу. Подробное описание "
    "сущностей (с типами полей) — в индивидуальной части системного "
    "аналитика; здесь — про РЕАЛИЗАЦИЮ. Объём 1 страница."
)

add_para(
    doc,
    "Доступ к базе данных организован через SQLAlchemy 2.x. Engine "
    "создаётся в app/db/session.py из строки подключения, заданной в "
    "переменной окружения DATABASE_URL (по умолчанию — sqlite-файл "
    "school_service.db в корне проекта). Реализована функция get_db() — "
    "FastAPI-зависимость, возвращающая сессию с автоматическим закрытием "
    "по окончании запроса."
)
add_para(
    doc,
    "ORM-модели в app/models/models.py объявляют четыре сущности с "
    "relationship-связями. Создание схемы выполняется на старте "
    "приложения через Base.metadata.create_all — для учебного "
    "прототипа этого достаточно; в направлениях развития предложено "
    "перейти на Alembic для управляемой миграции."
)
add_para(
    doc,
    "Принципиальное решение в части хранения — отдельное поле "
    "house_rule_raw, в котором сохраняется исходный текст правила из "
    "постановления. Это страховка на случай неполного или некорректного "
    "распознавания структурных полей: оператор сможет восстановить "
    "правило вручную через CRUD-таблицу админ-панели, опираясь на "
    "сырой текст."
)


# ─────────────────────── 3. Извлечение текста ────────────────────────────

add_heading(doc, "3. Извлечение текста из документов", level=1)

add_note(
    doc,
    "Описание модуля text_extractor и логики ветвления по форматам. "
    "Объём 1 страница."
)

add_para(
    doc,
    "Модуль app/services/parser/text_extractor.py реализует диспетчер по "
    "расширению файла. Для .docx используется python-docx, для .pdf — "
    "pdfplumber, для .doc — конвертация в .docx через LibreOffice "
    "headless (`soffice --convert-to docx`), а на macOS дополнительно "
    "поддерживается fallback на встроенный textutil."
)
add_para(
    doc,
    "Для PDF реализована двухступенчатая логика: сначала pdfplumber "
    "извлекает текстовый слой; если результат содержит менее 500 "
    "символов (критерий «практически пустой документ»), запускается "
    "OCR через сервис ocr.space. Граничное значение 500 символов "
    "выбрано по результатам экспериментов на реальных файлах: меньше — "
    "сканы без текстового слоя, больше — документы с нормальным "
    "текстом."
)
add_para(
    doc,
    "Извлечённый текст сохраняется в storage/extracted/ — это удобно "
    "при отладке стратегий парсинга: можно повторно прогнать парсер "
    "без переизвлечения текста из тяжёлого PDF."
)


# ─────────────────────── 4. Метаданные ────────────────────────────

add_heading(doc, "4. Извлечение метаданных постановления", level=1)

add_note(
    doc,
    "Модуль metadata_extractor.py: какие регэкспы, фильтр чужого "
    "контекста. Объём 0,5 страницы."
)

add_para(
    doc,
    "Модуль app/services/parser/metadata_extractor.py извлекает номер "
    "постановления, дату и наименование муниципалитета по первым 3000 "
    "символам документа. Используются четыре регулярных выражения: "
    "NUMBER_RE (для номера вида «№ 01-02-252»), DATE_NUMERIC_RE "
    "(числовая дата 06.03.2025), DATE_TEXT_RE (текстовая дата с "
    "месяцами в склонении), MUNICIPALITY_RE (наименование "
    "муниципалитета в нескольких типовых формах)."
)
add_para(
    doc,
    "Особое внимание уделено отсечению «чужого контекста». Постановления "
    "часто содержат ссылки на федеральные законы, приказы Минпросвещения "
    "и предыдущие отменённые постановления, реквизиты которых не должны "
    "интерпретироваться как метаданные текущего документа. Реализован "
    "фильтр _is_foreign_context, проверяющий 150 символов слева и 30 "
    "символов справа от найденного фрагмента на наличие маркеров «ФЗ», "
    "«закон», «приказ», «утративш(их) силу», «постановлени(е) "
    "правительств(а)»."
)


# ─────────────────────── 5. Структурные стратегии парсинга ────────────────────────────

add_heading(doc, "5. Структурный парсинг таблиц", level=1)

add_para(
    doc,
    "Реализованы две стратегии структурного разбора табличных частей "
    "постановлений, обе работают без обращения к большой языковой "
    "модели. Диспетчер universal_parser выбирает стратегию автоматически "
    "по структуре файла. Если ни одна структурная стратегия не "
    "распознала формат, выполняется fallback на LLM-парсер (реализация "
    "которого описана в индивидуальной части разработчика ИИ-агентов)."
)

add_heading(doc, "5.1. MultiColumnParserStrategy (Пермь-style)", level=2)
add_note(
    doc,
    "Описать алгоритм работы со множеством столбцов «№ | Школа | Улица | "
    "Дома». Подчеркнуть: эта стратегия НЕ вызывает LLM. Объём 0,5–1 стр."
)
add_para(
    doc,
    "Стратегия реализована в app/services/parser/strategies/"
    "multi_column_parser.py в связке с extractor'ом "
    "multi_column_extractor.py. Для PDF используется pdfplumber, для "
    "DOCX — python-docx. Извлечённые строки разбираются построчно: "
    "первый столбец содержит номер пункта, второй — имя школы (может "
    "быть пустым в continuation-строках), третий — улицу, четвёртый — "
    "описание домов."
)
add_para(
    doc,
    "Ключевая особенность реализации — пробрасывание имени школы между "
    "continuation-строками одной таблицы и даже между таблицами на "
    "разных страницах. Это позволяет корректно обрабатывать "
    "постановления, в которых имя школы указано только в первой "
    "строке группы."
)
add_para(
    doc,
    "Дополнительно стратегия поддерживает алиасы улиц вида "
    "«Монастырская (Орджоникидзе)» — для одной такой записи в "
    "результат добавляются два правила: с новым и со старым "
    "названиями. Вся стратегия работает без обращения к LLM, что "
    "обеспечивает мгновенную обработку постановлений Пермского края."
)

add_heading(doc, "5.2. TwoColumnParserStrategy (Балашиха-style)", level=2)
add_note(
    doc,
    "Структурная часть TwoColumnParserStrategy: извлечение таблицы "
    "(pdfplumber + 4 стратегии table_settings, fallback на PyMuPDF), "
    "склейка continuation-строк, merge_duplicate_schools, "
    "stitch_split_names. LLM-обработка ТЕКСТА территорий — это "
    "ответственность разработчика ИИ. Объём 1 страница."
)
add_para(
    doc,
    "Стратегия применяется для постановлений с длинным сплошным "
    "описанием территорий в одной ячейке (пример — постановление "
    "городского округа Балашиха). Структурная часть стратегии "
    "(до LLM) выполняет следующее:"
)
for item in [
    "извлечение таблицы из PDF: pdfplumber пробует 4 разных набора "
    "table_settings (text-based, lines-based, lines_strict, "
    "explicit_lines), выбирается вариант с наибольшим объёмом "
    "извлечённых строк по функции _score_rows;",
    "если pdfplumber дал мало строк — fallback на PyMuPDF (fitz);",
    "извлечение таблицы из DOCX через python-docx;",
    "склейка continuation-строк (когда продолжение территории «уехало» "
    "на следующую строку);",
    "сшивка разорванных между страницами имён школ "
    "(stitch_split_names);",
    "объединение дубликатов одной школы, если она встретилась дважды "
    "(merge_duplicate_schools).",
]:
    add_bullet(doc, item)
add_para(
    doc,
    "После того как структурная часть выделила пары «школа → "
    "territory_text», обработка территорий выполняется компонентами, "
    "относящимися к зоне ответственности разработчика ИИ-агентов "
    "(LLM-разбор + регэксповая страховка). Для каждой школы формируется "
    "финальный список правил, который через save_parsed_decree "
    "сохраняется в БД."
)

add_heading(doc, "5.3. Диспетчер universal_parser", level=2)
add_para(
    doc,
    "Модуль app/services/parser/universal_parser.py выполняет "
    "автоматический выбор стратегии. Сначала по структуре файла "
    "проверяется применимость MultiColumnParserStrategy "
    "(is_multi_column_format), затем — TwoColumnParserStrategy "
    "(_is_two_column_format). Если ни одна структурная стратегия не "
    "подходит, диспетчер передаёт документ в LLM-fallback. Такой "
    "порядок обеспечивает приоритет дешёвого и быстрого структурного "
    "разбора над дорогим LLM-разбором."
)


# ─────────────────────── 6. Нормализация правил ────────────────────────────

add_heading(doc, "6. Нормализация правил", level=1)

add_note(
    doc,
    "Модуль rule_normalizer.py: какие типы правил, как определяется "
    "parity, особый случай с «нечет». Объём 0,5–1 страница."
)

add_para(
    doc,
    "Модуль app/services/parser/rule_normalizer.py приводит «сырые» "
    "правила к каноническому виду: определяет тип правила (range, "
    "exact_list, up_to, from_to_end, all_except, all), извлекает "
    "диапазоны и списки домов, определяет чётность."
)
add_para(
    doc,
    "При реализации был выявлен тонкий момент: при определении "
    "чётности подстрока «чет» встречается и в слове «нечет». Поэтому "
    "сначала проверяется наличие «нечет/нечёт», затем оно вырезается "
    "из строки, и только потом проверяется оставшееся «чет/чёт». Это "
    "позволяет корректно различать правила «только нечётные», «только "
    "чётные» и «и чётные, и нечётные»."
)


# ─────────────────────── 7. Регэксповый fallback ────────────────────────────

add_heading(doc, "7. Регэксповый fallback по territory_text", level=1)

add_note(
    doc,
    "Модуль territory_regex_fallback.py. Этот модуль РАЗДЕЛЁН с "
    "разработчиком ИИ: сам регэкс-парсер написан как back-end-логика "
    "(тут), а его объединение с ответом LLM — в части разработчика ИИ. "
    "Объём 0,5 страницы."
)

add_para(
    doc,
    "Модуль app/services/parser/territory_regex_fallback.py выполняет "
    "независимый регэксповый разбор territory_text — сырого текста "
    "территории школы из двухколоночного постановления. Алгоритм "
    "получает на вход текст и регэкспом находит все упоминания улиц "
    "с описаниями домов; для каждой пары формируется «сырое» правило, "
    "которое далее проходит через rule_normalizer. Полученный набор "
    "правил передаётся компоненту merge_llm_with_fallback "
    "(ответственность разработчика ИИ-агентов), который объединяет "
    "результаты LLM и регэкса без дублирования."
)


# ─────────────────────── 8. Интеграция с DaData ────────────────────────────

add_heading(doc, "8. Интеграция с DaData", level=1)

add_note(
    doc,
    "Модули dadata/client.py и dadata/street_validator.py. "
    "Подчеркнуть устойчивость к недоступности и lru_cache. "
    "Объём 1 страница."
)

add_para(
    doc,
    "Модуль app/services/dadata/client.py предоставляет две функции — "
    "suggest_address (для автокомплита на главной странице) и "
    "clean_address (для нормализации полного адреса при поиске школы). "
    "Все вызовы обёрнуты в try/except: при недоступности DaData (нет "
    "ключа, TLS-таймаут, сетевой сбой) функция возвращает None, и "
    "вызывающий код переключается на локальный fallback-парсер."
)
add_para(
    doc,
    "Модуль app/services/dadata/street_validator.py выполняет сверку "
    "улицы при сохранении правила в БД. Поскольку при обработке "
    "постановления с 700+ правилами одна и та же улица встречается "
    "многократно, реализован lru_cache на функции _cached_suggest: "
    "повторные запросы по той же улице берутся из памяти. "
    "Это исключает сотни лишних HTTP-запросов и ускоряет сохранение "
    "постановления в десятки раз."
)
add_para(
    doc,
    "Принципиальное решение в части использования DaData при поиске "
    "школы: для матчинга используется поле rule.street, а не "
    "rule.normalized_street от DaData. При экспериментальной проверке "
    "выяснилось, что DaData в части случаев нормализует название улицы "
    "в посторонний объект (например, «шоссе Космонавтов» → «ГСК д-22 "
    "космонавтов»). Поэтому normalized_street сохраняется только для "
    "индексации и аудита, а матч выполняется по исходному названию с "
    "локальной нормализацией (см. раздел 10)."
)


# ─────────────────────── 9. Локальный парсер адреса ────────────────────────────

add_heading(doc, "9. Локальный fallback-парсер адреса", level=1)

add_note(
    doc,
    "Модуль local_parser.py — обеспечивает работу поиска при "
    "недоступной DaData. Объём 0,5 страницы."
)

add_para(
    doc,
    "Модуль app/services/address/local_parser.py — резервный парсер "
    "адреса на случай недоступности DaData. На вход принимает строку "
    "адреса от пользователя, регэкспами извлекает компоненты "
    "населённого пункта, улицы и номера дома, возвращает структуру, "
    "совместимую с ответом DaData. Это позволяет функции поиска "
    "работать даже при отвалившемся внешнем сервисе, обеспечивая "
    "выполнение нефункционального требования NF-02."
)


# ─────────────────────── 10. Поиск школы ────────────────────────────

add_heading(doc, "10. Алгоритм поиска школы по адресу", level=1)

add_note(
    doc,
    "Модуль find_school.py. Три фильтра, толерантный матч локалити. "
    "Объём 1–1,5 страницы."
)

add_para(
    doc,
    "Модуль app/services/search/find_school.py содержит функцию "
    "find_school_by_address. Используется простой линейный обход всех "
    "правил из базы — для учебного масштаба (≈ 700–1000 правил) это "
    "укладывается в требование NF-01 (отклик не более 1 с)."
)
add_para(doc, "Для каждого правила выполняются три проверки:",
         first_line_indent=0)
for item in [
    "совпадение нормализованной улицы (normalize_street_name снимает "
    "префиксы «ул.», «пр-кт», «шоссе», «мкр.» и т. п. с обеих сторон "
    "сравнения с использованием границ слова \\b);",
    "толерантный матч локалити (_locality_match по общему префиксу не "
    "менее 4 символов и допуску расхождения хвоста до 2 символов);",
    "проверка попадания дома под правило (is_house_matches учитывает "
    "exact_list, exceptions, parity, range, up_to, from_to_end).",
]:
    add_bullet(doc, item)

add_para(
    doc,
    "Толерантный матч локалити (_locality_match) — авторская техника, "
    "позволяющая сопоставлять варианты с разными падежами и буквой «ё»: "
    "«Пермь» / «Перми», «Берёзники» / «Березников», «Балашиха» / "
    "«Балашихи». Алгоритм считает общий префикс символов: при общем "
    "префиксе не менее 4 символов и расхождении в конце не более 2 "
    "символов локалити считаются совпадающими. При этом «Балашиха» и "
    "«Балахна», «Пермь» и «Тверь», «Москва» и «Минск» — корректно НЕ "
    "сопоставляются."
)


# ─────────────────────── 11. Валидатор правил ────────────────────────────

add_heading(doc, "11. Валидатор правил", level=1)

add_note(
    doc,
    "Модуль rules_validator.py: 5 проверок, алгоритм поиска "
    "пересечений. Объём 0,5–1 страница."
)

add_para(
    doc,
    "Модуль app/services/validation/rules_validator.py реализует пять "
    "проверок целостности данных:"
)
add_table_simple(doc, [
    ["Проверка", "Уровень", "Описание"],
    ["empty_street", "error", "У правила не указана улица"],
    ["empty_house_rule", "warning", "У правила пустой текст описания домов"],
    ["invalid_range", "error", "Перевёрнут диапазон: house_from > house_to"],
    ["unknown_parity", "warning",
     "Значение parity вне допустимого списка"],
    ["intersection", "error",
     "Два правила разных школ покрывают один и тот же дом на одной улице"],
])
add_para(
    doc,
    "Алгоритм поиска пересечений работает следующим образом: правила "
    "группируются по ключу «локалити:нормализованная_улица», внутри "
    "каждой группы попарно сравниваются множества домов, вычисляемые "
    "функцией rule_to_numbers. Если пересечение непустое и правила "
    "принадлежат разным школам — фиксируется ошибка с превью "
    "пересекающихся номеров домов."
)


# ─────────────────────── 12. HTTP-эндпоинты и шаблоны ────────────────────────────

add_heading(doc, "12. HTTP-эндпоинты и шаблоны Jinja2", level=1)

add_note(
    doc,
    "Описание реализованных эндпоинтов FastAPI и шаблонов Jinja2. "
    "Объём 1 страница."
)

add_para(
    doc,
    "Реализованы публичные эндпоинты для родителя и административные "
    "эндпоинты для сотрудника УО. Все эндпоинты возвращают HTML, "
    "сгенерированный шаблонами Jinja2 (server-side rendering); эндпоинт "
    "автодополнения адреса возвращает JSON. Перечень эндпоинтов "
    "приведён в таблице 12.1."
)
add_para(doc, "Таблица 12.1 — Реализованные HTTP-эндпоинты",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["Метод", "Путь", "Описание"],
    ["GET",   "/",                            "Главная страница с формой поиска"],
    ["POST",  "/search",                      "Поиск школы по адресу"],
    ["GET",   "/api/address/suggest",         "Прокси к DaData suggest"],
    ["GET",   "/admin/upload",                "Страница загрузки постановления"],
    ["POST",  "/admin/upload",                "Загрузка файла и извлечение текста"],
    ["POST",  "/admin/parse-with-ai",         "Запуск парсинга по тексту"],
    ["POST",  "/admin/save-parsed",           "Сохранение результата в БД"],
    ["GET",   "/admin/rules",                 "CRUD-таблица правил"],
    ["POST",  "/admin/rules/update/{id}",     "Обновление правила"],
    ["POST",  "/admin/rules/delete/{id}",     "Удаление правила"],
    ["GET",   "/admin/validation",            "Отчёт валидатора"],
])

add_para(
    doc,
    "Реализованы четыре HTML-шаблона: index.html (форма поиска), "
    "admin_upload.html (загрузка и предпросмотр распарсенного), "
    "admin_rules.html (CRUD-таблица), admin_validation.html (отчёт "
    "валидатора). Автодокументация API доступна стандартными "
    "эндпоинтами FastAPI: /docs (Swagger UI) и /redoc."
)


# ─────────────────────── 13. CLI-утилиты ────────────────────────────

add_heading(doc, "13. Утилиты командной строки", level=1)

add_note(
    doc,
    "Описание трёх CLI-утилит: seed.py, reset_db.py, inspect_db.py. "
    "Объём 0,5 страницы."
)

add_para(doc, "Для удобства разработки и отладки реализованы три CLI-утилиты:",
         first_line_indent=0)
for item in [
    "seed.py — заполнение БД минимальным тестовым набором данных "
    "(одна школа г. Берёзники);",
    "reset_db.py — полная очистка БД с последующим вызовом seed (для "
    "сброса состояния между прогонами);",
    "inspect_db.py — инспектор содержимого БД: без аргументов выводит "
    "сводку по числу муниципалитетов/школ/правил; с аргументом-улицей "
    "— список правил по улице; с флагом --school — все правила школы; "
    "с флагом --check — симуляция поиска по адресу.",
]:
    add_bullet(doc, item)


# ─────────────────────── 14. Конфигурация и развёртывание ────────────────────────────

add_heading(doc, "14. Конфигурация и развёртывание", level=1)

add_note(
    doc,
    "Описать app/core/config.py, .env, требования к окружению, "
    "запуск через uvicorn. Объём 1 страница."
)

add_para(
    doc,
    "Конфигурация приложения вынесена в модуль app/core/config.py на "
    "основе pydantic-settings. Все секреты (токены DaData, GigaChat, "
    "ocr.space) читаются из переменных окружения; локально — из "
    "файла .env. Перечень переменных окружения приведён в таблице 14.1."
)
add_para(doc, "Таблица 14.1 — Переменные окружения",
         first_line_indent=0, italic=True)
add_table_simple(doc, [
    ["Переменная", "Назначение"],
    ["DATABASE_URL",            "Строка подключения к БД (по умолчанию — sqlite:///./school_service.db)"],
    ["DADATA_TOKEN",            "Токен DaData"],
    ["DADATA_SECRET",           "Секрет DaData"],
    ["GIGACHAT_CREDENTIALS",    "Authorization key для GigaChat"],
    ["GIGACHAT_VERIFY_SSL_CERTS","Проверка SSL-сертификатов GigaChat"],
    ["OCR_SPACE_API_KEY",       "API-ключ ocr.space"],
])
add_para(
    doc,
    "Реализована особенность: относительный путь "
    "sqlite:///./school_service.db автоматически резолвится в "
    "абсолютный относительно корня проекта (см. app/core/config.py). "
    "Это нужно для того, чтобы uvicorn в режиме --reload не «терял» БД "
    "при смене рабочей директории."
)
add_para(doc, "Для запуска прототипа необходимы следующие системные зависимости:",
         first_line_indent=0)
for item in [
    "Python 3.11 или новее с установленными пакетами из requirements.txt;",
    "LibreOffice в headless-режиме (для конвертации .doc → .docx); на "
    "Linux обязательно, на macOS опционально (есть fallback на textutil);",
    "доступ к интернету для обращений к DaData, GigaChat и ocr.space "
    "(при их недоступности приложение продолжит работать в "
    "ограниченном режиме за счёт реализованных fallback-ов).",
]:
    add_bullet(doc, item)
add_para(doc, "Запуск приложения:", first_line_indent=0)
add_para(doc, "uvicorn app.main:app --reload", italic=True,
         first_line_indent=0)


# ─────────────────────── 15. Заключение ────────────────────────────

add_heading(doc, "15. Заключение", level=1)

add_para(
    doc,
    "В индивидуальной части разработчика сервиса представлена "
    "реализация серверной части веб-приложения: каркас на FastAPI, "
    "модель данных на SQLAlchemy, конвейер извлечения текста, "
    "структурные стратегии парсинга таблиц без обращения к большой "
    "языковой модели, нормализация правил, регэксповый fallback по "
    "territory_text, интеграция с DaData, локальный fallback-парсер "
    "адреса, алгоритм поиска школы с авторской техникой толерантного "
    "матча локалити, валидатор правил, HTML-шаблоны административной "
    "панели, CLI-утилиты и конфигурация. Реализованная серверная часть "
    "обеспечивает выполнение всех функциональных и нефункциональных "
    "требований, относящихся к зоне ответственности разработчика "
    "сервиса. Вопросы интеграции с большой языковой моделью и "
    "тестирования системы рассмотрены в соответствующих индивидуальных "
    "частях."
)


doc.save(REPORT_PATH)
print(f"Готово: {REPORT_PATH}")
